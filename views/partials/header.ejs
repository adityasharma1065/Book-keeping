<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book-keeping</title>
    <link rel="icon" type="image/x-icon" href="https://ssl.gstatic.com/docs/spreadsheets/forms/favicon_qp2.png">

    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="navbar flex justify-between w-full px-20 py-10">
        <h3>Book-keeping</h3>
        <%var errmsg=typeof errmsg=="undefined"? []:errmsg %>
        <%if(errmsg.length){%>
        <div class="absolute top-10 left-1/2 -translate-x-1/2 -translate-y-1/2 px-3 py-2 bg-red-500 rounded-md">
            <h3 class="text-red-200">
                <%=errmsg%>
            </h3>
        </div> 
        <%}%>

        <%var nav=typeof nav=="undefined" ? true:nav%>
        <%if(nav){%>
        <div class="links flex gap-4">
            <a href="/profile">Home</a>
            <a href="/hisaab/create">Create new record</a>
            <a class="text-red-500" href="/logout">Logout</a>
        </div>
        <%}%>
    </div>